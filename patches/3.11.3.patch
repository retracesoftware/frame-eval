diff --git a/Include/internal/pycore_pystate.h b/Include/internal/pycore_pystate.h
index cb8f115768a35205cc273d3618422e6a83368370..e077df18ad2049766a02e35e4c12787d6541e217 100644
--- a/Include/internal/pycore_pystate.h
+++ b/Include/internal/pycore_pystate.h
@@ -82,8 +82,13 @@ _PyRuntimeState_GetThreadState(_PyRuntimeState *runtime)
 static inline PyThreadState*
 _PyThreadState_GET(void)
 {
-    return _PyRuntimeState_GetThreadState(&_PyRuntime);
+    return _PyThreadState_UncheckedGet();
 }
+#define FRAME_EVAL_SPECIALIZATION_ALLOWED_INTERP(interp) \
+    ((interp)->eval_frame == NULL || \
+     (interp)->eval_frame == _PyEval_EvalFrameDefault)
+#define FRAME_EVAL_SPECIALIZATION_ALLOWED(tstate) \
+    FRAME_EVAL_SPECIALIZATION_ALLOWED_INTERP((tstate)->interp)
 
 PyAPI_FUNC(void) _Py_NO_RETURN _Py_FatalError_TstateNULL(const char *func);
 
diff --git a/Objects/dictobject.c b/Objects/dictobject.c
index 69a4a865103c182bc978353c83ea926dd988beb8..23b6121b5abf74170649a03639502964246cb7eb 100644
--- a/Objects/dictobject.c
+++ b/Objects/dictobject.c
@@ -450,19 +450,9 @@ estimate_log2_keysize(Py_ssize_t n)
 /* This immutable, empty PyDictKeysObject is used for PyDict_Clear()
  * (which cannot fail and thus can do no allocation).
  */
-static PyDictKeysObject empty_keys_struct = {
-        1, /* dk_refcnt */
-        0, /* dk_log2_size */
-        0, /* dk_log2_index_bytes */
-        DICT_KEYS_UNICODE, /* dk_kind */
-        1, /* dk_version */
-        0, /* dk_usable (immutable) */
-        0, /* dk_nentries */
-        {DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY,
-         DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY}, /* dk_indices */
-};
+extern PyDictKeysObject *frame_eval_empty_keys;
 
-#define Py_EMPTY_KEYS &empty_keys_struct
+#define Py_EMPTY_KEYS frame_eval_empty_keys
 
 /* Uncomment to check the dict content in _PyDict_CheckConsistency() */
 // #define DEBUG_PYDICT
diff --git a/Objects/unicodeobject.c b/Objects/unicodeobject.c
index 84d17f000b41e4a58569f765368f740fa7483516..71efc38c7a367edc99994fa9cb2b983ee61d9b7f 100644
--- a/Objects/unicodeobject.c
+++ b/Objects/unicodeobject.c
@@ -15397,7 +15397,7 @@ void
 _PyUnicode_ExactDealloc(PyObject *op)
 {
     assert(PyUnicode_CheckExact(op));
-    unicode_dealloc(op);
+    Py_TYPE(op)->tp_dealloc(op);
 }
 
 PyDoc_STRVAR(unicode_doc,
diff --git a/Objects/unionobject.c b/Objects/unionobject.c
index 5eee27c08fa7e20609468fbefa6f9a05369fde0c..48f611c090469ddb4baad88bb1526c6983f24d85 100644
--- a/Objects/unionobject.c
+++ b/Objects/unionobject.c
@@ -4,6 +4,8 @@
 #include "pycore_unionobject.h"
 #include "structmember.h"
 
+extern PyTypeObject *frame_eval_union_type;
+
 
 static PyObject *make_union(PyObject *);
 
@@ -395,7 +397,7 @@ make_union(PyObject *args)
 {
     assert(PyTuple_CheckExact(args));
 
-    unionobject *result = PyObject_GC_New(unionobject, &_PyUnion_Type);
+    unionobject *result = PyObject_GC_New(unionobject, frame_eval_union_type);
     if (result == NULL) {
         return NULL;
     }
diff --git a/Python/ceval.c b/Python/ceval.c
index 72f9c8375d072ac28faa92b2805749033b254cc7..e0f8237fffb8333c458d627f23805da214b9648a 100644
--- a/Python/ceval.c
+++ b/Python/ceval.c
@@ -35,6 +35,31 @@
 #include <ctype.h>
 #include <stdbool.h>
 
+#ifndef FRAME_EVAL_HOOK_EVAL_ENTER
+#  define FRAME_EVAL_HOOK_EVAL_ENTER() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_EVAL_EXIT
+#  define FRAME_EVAL_HOOK_EVAL_EXIT() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_INSTRUCTION
+#  define FRAME_EVAL_HOOK_INSTRUCTION() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_BACKWARD_JUMP
+#  define FRAME_EVAL_HOOK_BACKWARD_JUMP() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_FRAME_PUSH
+#  define FRAME_EVAL_HOOK_FRAME_PUSH(new_frame) ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_FRAME_POP
+#  define FRAME_EVAL_HOOK_FRAME_POP() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_NATIVE_CALL_PRE
+#  define FRAME_EVAL_HOOK_NATIVE_CALL_PRE() ((void)0)
+#endif
+#ifndef FRAME_EVAL_HOOK_NATIVE_CALL_POST
+#  define FRAME_EVAL_HOOK_NATIVE_CALL_POST() ((void)0)
+#endif
+
 #ifdef Py_DEBUG
    /* For debugging the interpreter: */
 #  define LLTRACE  1      /* Low-level trace feature */
@@ -1201,18 +1226,13 @@ eval_frame_handle_pending(PyThreadState *tstate)
     /* GIL drop request */
     if (_Py_atomic_load_relaxed_int32(&ceval2->gil_drop_request)) {
         /* Give another thread a chance */
-        if (_PyThreadState_Swap(&runtime->gilstate, NULL) != tstate) {
+        if (PyEval_SaveThread() != tstate) {
             Py_FatalError("tstate mix-up");
         }
-        drop_gil(ceval, ceval2, tstate);
 
         /* Other threads may run now */
 
-        take_gil(tstate);
-
-        if (_PyThreadState_Swap(&runtime->gilstate, tstate) != NULL) {
-            Py_FatalError("orphan tstate");
-        }
+        PyEval_RestoreThread(tstate);
     }
 
     /* Check for asynchronous exception. */
@@ -1303,12 +1323,17 @@ eval_frame_handle_pending(PyThreadState *tstate)
 #define INSTRUCTION_START(op) \
     do { \
         frame->prev_instr = next_instr++; \
+        FRAME_EVAL_HOOK_INSTRUCTION(); \
         OPCODE_EXE_INC(op); \
         _py_stats.opcode_stats[lastopcode].pair_count[op]++; \
         lastopcode = op; \
     } while (0)
 #else
-#define INSTRUCTION_START(op) (frame->prev_instr = next_instr++)
+#define INSTRUCTION_START(op) \
+    do { \
+        frame->prev_instr = next_instr++; \
+        FRAME_EVAL_HOOK_INSTRUCTION(); \
+    } while (0)
 #endif
 
 #if USE_COMPUTED_GOTOS
@@ -1671,6 +1696,8 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
     /* Push frame */
     frame->previous = prev_cframe->current_frame;
     cframe.current_frame = frame;
+    FRAME_EVAL_HOOK_EVAL_ENTER();
+    FRAME_EVAL_HOOK_FRAME_PUSH(frame);
 
     /* support for generator.throw() */
     if (throwflag) {
@@ -2265,6 +2292,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             JUMPBY(INLINE_CACHE_ENTRIES_BINARY_SUBSCR);
             frame->prev_instr = next_instr - 1;
             new_frame->previous = frame;
+            FRAME_EVAL_HOOK_FRAME_PUSH(new_frame);
             frame = cframe.current_frame = new_frame;
             CALL_STAT_INC(inlined_py_calls);
             goto start_frame;
@@ -2433,6 +2461,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             TRACE_FUNCTION_EXIT();
             DTRACE_FUNCTION_EXIT();
             _Py_LeaveRecursiveCallTstate(tstate);
+            FRAME_EVAL_HOOK_FRAME_POP();
             if (!frame->is_entry) {
                 // GH-99729: We need to unlink the frame *before* clearing it:
                 _PyInterpreterFrame *dying = frame;
@@ -2446,6 +2475,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             tstate->cframe->use_tracing = cframe.use_tracing;
             assert(tstate->cframe->current_frame == frame->previous);
             assert(!_PyErr_Occurred(tstate));
+            FRAME_EVAL_HOOK_EVAL_EXIT();
             return retval;
         }
 
@@ -2648,11 +2678,13 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             TRACE_FUNCTION_EXIT();
             DTRACE_FUNCTION_EXIT();
             _Py_LeaveRecursiveCallTstate(tstate);
+            FRAME_EVAL_HOOK_FRAME_POP();
             /* Restore previous cframe and return. */
             tstate->cframe = cframe.previous;
             tstate->cframe->use_tracing = cframe.use_tracing;
             assert(tstate->cframe->current_frame == frame->previous);
             assert(!_PyErr_Occurred(tstate));
+            FRAME_EVAL_HOOK_EVAL_EXIT();
             return retval;
         }
 
@@ -4009,6 +4041,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             }
             if (Py_IsFalse(cond)) {
                 _Py_DECREF_NO_DEALLOC(cond);
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
                 DISPATCH();
@@ -4018,6 +4051,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             if (err > 0)
                 ;
             else if (err == 0) {
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
             }
@@ -4058,6 +4092,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             }
             if (Py_IsTrue(cond)) {
                 _Py_DECREF_NO_DEALLOC(cond);
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
                 DISPATCH();
@@ -4065,6 +4100,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             int err = PyObject_IsTrue(cond);
             Py_DECREF(cond);
             if (err > 0) {
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
             }
@@ -4102,6 +4138,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             PyObject *value = POP();
             if (!Py_IsNone(value)) {
                 Py_DECREF(value);
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
                 DISPATCH();
@@ -4123,6 +4160,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             PyObject *value = POP();
             if (Py_IsNone(value)) {
                 _Py_DECREF_NO_DEALLOC(value);
+                FRAME_EVAL_HOOK_BACKWARD_JUMP();
                 JUMPBY(-oparg);
                 CHECK_EVAL_BREAKER();
             }
@@ -4199,6 +4237,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
              * generator or coroutine, so we deliberately do not check it here.
              * (see bpo-30039).
              */
+            FRAME_EVAL_HOOK_BACKWARD_JUMP();
             JUMPBY(-oparg);
             DISPATCH();
         }
@@ -4206,6 +4245,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
         TARGET(JUMP_BACKWARD_QUICK) {
             PREDICTED(JUMP_BACKWARD_QUICK);
             assert(oparg < INSTR_OFFSET());
+            FRAME_EVAL_HOOK_BACKWARD_JUMP();
             JUMPBY(-oparg);
             CHECK_EVAL_BREAKER();
             DISPATCH();
@@ -4739,7 +4779,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             PyObject *function = PEEK(total_args + 1);
             int positional_args = total_args - KWNAMES_LEN();
             // Check if the call can be inlined or not
-            if (Py_TYPE(function) == &PyFunction_Type && tstate->interp->eval_frame == NULL) {
+            if (Py_TYPE(function) == &PyFunction_Type && FRAME_EVAL_SPECIALIZATION_ALLOWED(tstate)) {
                 int code_flags = ((PyCodeObject*)PyFunction_GET_CODE(function))->co_flags;
                 PyObject *locals = code_flags & CO_OPTIMIZED ? NULL : PyFunction_GET_GLOBALS(function);
                 STACK_SHRINK(total_args);
@@ -4758,12 +4798,14 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                 JUMPBY(INLINE_CACHE_ENTRIES_CALL);
                 frame->prev_instr = next_instr - 1;
                 new_frame->previous = frame;
+                FRAME_EVAL_HOOK_FRAME_PUSH(new_frame);
                 cframe.current_frame = frame = new_frame;
                 CALL_STAT_INC(inlined_py_calls);
                 goto start_frame;
             }
             /* Callable is not a normal Python function */
             PyObject *res;
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             if (cframe.use_tracing) {
                 res = trace_call_function(
                     tstate, function, stack_pointer-total_args,
@@ -4775,6 +4817,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                     positional_args | PY_VECTORCALL_ARGUMENTS_OFFSET,
                     call_shape.kwnames);
             }
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             call_shape.kwnames = NULL;
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             Py_DECREF(function);
@@ -4839,7 +4882,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
 
         TARGET(CALL_PY_EXACT_ARGS) {
             assert(call_shape.kwnames == NULL);
-            DEOPT_IF(tstate->interp->eval_frame, CALL);
+            DEOPT_IF(!FRAME_EVAL_SPECIALIZATION_ALLOWED(tstate), CALL);
             _PyCallCache *cache = (_PyCallCache *)next_instr;
             int is_meth = is_method(stack_pointer, oparg);
             int argcount = oparg + is_meth;
@@ -4867,13 +4910,14 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             JUMPBY(INLINE_CACHE_ENTRIES_CALL);
             frame->prev_instr = next_instr - 1;
             new_frame->previous = frame;
+            FRAME_EVAL_HOOK_FRAME_PUSH(new_frame);
             frame = cframe.current_frame = new_frame;
             goto start_frame;
         }
 
         TARGET(CALL_PY_WITH_DEFAULTS) {
             assert(call_shape.kwnames == NULL);
-            DEOPT_IF(tstate->interp->eval_frame, CALL);
+            DEOPT_IF(!FRAME_EVAL_SPECIALIZATION_ALLOWED(tstate), CALL);
             _PyCallCache *cache = (_PyCallCache *)next_instr;
             int is_meth = is_method(stack_pointer, oparg);
             int argcount = oparg + is_meth;
@@ -4909,6 +4953,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             JUMPBY(INLINE_CACHE_ENTRIES_CALL);
             frame->prev_instr = next_instr - 1;
             new_frame->previous = frame;
+            FRAME_EVAL_HOOK_FRAME_PUSH(new_frame);
             frame = cframe.current_frame = new_frame;
             goto start_frame;
         }
@@ -4985,8 +5030,10 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             STAT_INC(PRECALL, hit);
             SKIP_CALL();
             STACK_SHRINK(total_args);
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = tp->tp_vectorcall((PyObject *)tp, stack_pointer,
                                               total_args-kwnames_len, call_shape.kwnames);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             call_shape.kwnames = NULL;
             /* Free the arguments. */
             for (int i = 0; i < total_args; i++) {
@@ -5021,7 +5068,9 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                 goto error;
             }
             PyObject *arg = TOP();
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = _PyCFunction_TrampolineCall(cfunc, PyCFunction_GET_SELF(callable), arg);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             _Py_LeaveRecursiveCallTstate(tstate);
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
 
@@ -5051,10 +5100,12 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             PyCFunction cfunc = PyCFunction_GET_FUNCTION(callable);
             STACK_SHRINK(total_args);
             /* res = func(self, args, nargs) */
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = ((_PyCFunctionFast)(void(*)(void))cfunc)(
                 PyCFunction_GET_SELF(callable),
                 stack_pointer,
                 total_args);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
 
             /* Free the arguments. */
@@ -5092,12 +5143,14 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             _PyCFunctionFastWithKeywords cfunc =
                 (_PyCFunctionFastWithKeywords)(void(*)(void))
                 PyCFunction_GET_FUNCTION(callable);
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = cfunc(
                 PyCFunction_GET_SELF(callable),
                 stack_pointer,
                 total_args - KWNAMES_LEN(),
                 call_shape.kwnames
             );
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             call_shape.kwnames = NULL;
 
@@ -5222,7 +5275,9 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             if (_Py_EnterRecursiveCallTstate(tstate, " while calling a Python object")) {
                 goto error;
             }
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = _PyCFunction_TrampolineCall(cfunc, self, arg);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             _Py_LeaveRecursiveCallTstate(tstate);
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             Py_DECREF(self);
@@ -5254,8 +5309,10 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             STACK_SHRINK(nargs);
             _PyCFunctionFastWithKeywords cfunc =
                 (_PyCFunctionFastWithKeywords)(void(*)(void))meth->ml_meth;
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = cfunc(self, stack_pointer, nargs - KWNAMES_LEN(),
                                   call_shape.kwnames);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             call_shape.kwnames = NULL;
 
@@ -5294,7 +5351,9 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             if (_Py_EnterRecursiveCallTstate(tstate, " while calling a Python object")) {
                 goto error;
             }
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = _PyCFunction_TrampolineCall(cfunc, self, NULL);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             _Py_LeaveRecursiveCallTstate(tstate);
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             Py_DECREF(self);
@@ -5326,7 +5385,9 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                 (_PyCFunctionFast)(void(*)(void))meth->ml_meth;
             int nargs = total_args-1;
             STACK_SHRINK(nargs);
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             PyObject *res = cfunc(self, stack_pointer, nargs);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             assert((res != NULL) ^ (_PyErr_Occurred(tstate) != NULL));
             /* Clear the stack of the arguments. */
             for (int i = 0; i < nargs; i++) {
@@ -5377,7 +5438,9 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             }
             assert(PyTuple_CheckExact(callargs));
 
+            FRAME_EVAL_HOOK_NATIVE_CALL_PRE();
             result = do_call_core(tstate, func, callargs, kwargs, cframe.use_tracing);
+            FRAME_EVAL_HOOK_NATIVE_CALL_POST();
             Py_DECREF(func);
             Py_DECREF(callargs);
             Py_XDECREF(kwargs);
@@ -5436,6 +5499,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             gen->gi_frame_state = FRAME_CREATED;
             gen_frame->owner = FRAME_OWNED_BY_GENERATOR;
             _Py_LeaveRecursiveCallTstate(tstate);
+            FRAME_EVAL_HOOK_FRAME_POP();
             if (!frame->is_entry) {
                 _PyInterpreterFrame *prev = frame->previous;
                 _PyThreadState_PopFrame(tstate, frame);
@@ -5453,6 +5517,7 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
             tstate->cframe->use_tracing = cframe.use_tracing;
             assert(tstate->cframe->current_frame == frame->previous);
             assert(!_PyErr_Occurred(tstate));
+            FRAME_EVAL_HOOK_EVAL_EXIT();
             return (PyObject *)gen;
         }
 
@@ -5822,11 +5887,13 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
 exit_unwind:
     assert(_PyErr_Occurred(tstate));
     _Py_LeaveRecursiveCallTstate(tstate);
+    FRAME_EVAL_HOOK_FRAME_POP();
     if (frame->is_entry) {
         /* Restore previous cframe and exit */
         tstate->cframe = cframe.previous;
         tstate->cframe->use_tracing = cframe.use_tracing;
         assert(tstate->cframe->current_frame == frame->previous);
+        FRAME_EVAL_HOOK_EVAL_EXIT();
         return NULL;
     }
     // GH-99729: We need to unlink the frame *before* clearing it:
diff --git a/Python/specialize.c b/Python/specialize.c
index 08ce2f5caa6bd909e455da9cb3b7d03d0af66ab9..2bc6df4098d2046264dc1adc7059052f73ada46d 100644
--- a/Python/specialize.c
+++ b/Python/specialize.c
@@ -1482,7 +1482,7 @@ specialize_py_call(PyFunctionObject *func, _Py_CODEUNIT *instr, int nargs,
     PyCodeObject *code = (PyCodeObject *)func->func_code;
     int kind = function_kind(code);
     /* Don't specialize if PEP 523 is active */
-    if (_PyInterpreterState_GET()->eval_frame) {
+    if (!FRAME_EVAL_SPECIALIZATION_ALLOWED_INTERP(_PyInterpreterState_GET())) {
         SPECIALIZATION_FAIL(CALL, SPEC_FAIL_CALL_PEP_523);
         return -1;
     }
