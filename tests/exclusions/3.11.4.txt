# CPython's perf trampoline is itself an exclusive PEP 523 eval-frame hook.
# It cannot be active while frame_eval is installed.
test_perf_profiler

# test___all__ imports a file with an invalid escape sequence fixed in 3.11.6.
test___all__

# test_freeze rebuilds the patched source tree without the generated archive.
# CPython 3.11.6 marks this slow test as requiring the CPU resource.
test_tools
