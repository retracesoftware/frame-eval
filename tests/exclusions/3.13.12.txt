# CPython's perf trampoline is itself an exclusive PEP 523 eval-frame hook.
# It cannot be active while frame_eval is installed.
test_perf_profiler

# Fragile under load due to a hardcoded 1 ms timeout; see python/cpython#119452.
test_httpservers.CGIHTTPServerTestCase.test_large_content_length_truncated
