# CPython's perf trampoline is itself an exclusive PEP 523 eval-frame hook.
# It cannot be active while frame_eval is installed.
test_perf_profiler

# These CPython process-pool tests deadlock during shutdown with frame_eval.
test.test_concurrent_futures.test_deadlock
test.test_concurrent_futures.test_shutdown
